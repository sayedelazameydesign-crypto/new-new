# خطة النشر المجاني — مِحور

## النطاق والافتراضات

تهدف هذه الخطة إلى نشر التطبيق بأقل تكلفة ممكنة ومن دون افتراض إدخال بطاقة ائتمان. لا يمكن ضمان عدم طلب بطاقة على نحو مطلق؛ الصياغة الدقيقة هي: **لا يُتوقع طلب بطاقة ضمن الخطة المجانية الظاهرة حاليًا، لكن شروط المنصة وحدودها قد تختلف حسب الحساب أو تتغير لاحقًا**.

## المتطلبات السابقة

يجب استخدام النسخة المستقرة في checkpoint `5974f898`، مع توثيق أن GitHub قد يحتوي نسخة أقدم ما لم يُرفع هذا checkpoint إلى الفرع `main`. يجب ضبط متغيرات المصادقة وقاعدة البيانات الخاصة ببيئة النشر من لوحة المشروع، وعدم وضع أي سر داخل الواجهة أو مستودع GitHub.

## مفاتيح الذكاء الاصطناعي

المسار الافتراضي للمستخدم هو إدخال مفتاح Google أو Groq أو Hugging Face من **إعدادات المزودين** داخل التطبيق. تُشفّر المفاتيح في قاعدة البيانات ولا تُعاد إلى المتصفح. أما `GEMINI_API_KEY` العام فهو متغير خادمي اختياري للاختبار أو التشغيل الإداري، وليس المسار الافتراضي لمفاتيح المستخدمين.

## بوابة الجودة قبل النشر

يجب تنفيذ الأوامر التالية من جذر المشروع:

```bash
pnpm check
pnpm test
NODE_OPTIONS=--max-old-space-size=1024 pnpm build
pnpm audit --prod --audit-level high
```

لا يُعتمد النشر العام مع وجود ثغرات حرجة أو عالية غير مقبولة. إذا بقيت الثغرات في تبعيات عابرة مثل AWS أو Express أو Mermaid بعد الترقيات الآمنة، يجب تسجيلها مع الحزمة الأم، وفحص ما إذا كان الإصلاح يتطلب ترقية كبرى أو استبدال التبعية، بدل فرض override غير متوافق دون اختبار.

## خطوات النشر

افتح النسخة المستقرة من Management UI، راجع الأسرار والإعدادات، ثم اضغط **Publish** يدويًا. بعد إنشاء الرابط العام، اختبر تسجيل الدخول، إنشاء المحادثة، حذفها، إعادة فتحها، البث التدريجي، الأوامر العربية، تنظيف `/شرح /شرح`، Markdown، RTL، وعدم ظهور الأسرار.

## التحليلات والخصوصية

يجب ضبط `VITE_ANALYTICS_ENDPOINT` و`VITE_ANALYTICS_WEBSITE_ID` إلى خدمة تحليلات مقصودة قبل النشر، مع مراجعة سياسة الخصوصية. إذا لم تكن التحليلات مطلوبة، يُزال Script التحليلات من `client/index.html` بدل ترك قيم placeholder.

## قرار الإطلاق

يُسمح بالإطلاق عندما تنجح فحوص TypeScript والاختبارات وbuild، وتكون الثغرات الحرجة والعالية معالجة أو موثقة ومقبولة صراحة، ويكون مصدر GitHub متطابقًا مع checkpoint المراد نشره أو موضحًا بأنه أقدم. لا يتم تنفيذ Publish تلقائيًا من الطرفية؛ يضغط صاحب المشروع زر **Publish** من الواجهة بعد المراجعة.
