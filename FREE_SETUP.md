# مِحور — دليل التشغيل المجاني

مِحور هو مساعد عربي تفاعلي يعمل بواجهة RTL داكنة، ويدعم المحادثات المتعددة، Markdown، الأوامر السريعة، البث التدريجي، والذاكرة الدائمة. يعتمد التشغيل المجاني على مفاتيح API الشخصية التي يضيفها المستخدم من لوحة الإعدادات؛ لا تُحفظ المفاتيح في المتصفح، بل تُشفّر على الخادم باستخدام مفتاح جلسة المشروع.

## البدء

شغّل المشروع بالأوامر التالية:

```bash
pnpm install
pnpm dev
```

بعد تسجيل الدخول، أنشئ محادثة، اختر النموذج، ثم افتح **إعدادات المزودين** وأضف مفتاح Google أو Groq. لا تضع المفاتيح في ملفات الواجهة ولا في مستودع GitHub.

## النماذج

| النموذج | المزود | الاستخدام |
|---|---|---|
| Gemini 2.5 Flash | Google | المحادثات اليومية |
| Llama 3.3 70B | Groq | التحليل والسرعة |
| Mixtral 8x7B | Groq | النصوص الطويلة |

تختلف الحصص المجانية وسياسات التسجيل بين المزودين وقد تتغير بمرور الوقت. يطبق الخادم سياسة retry محددة داخل كل طلب: محاولتان كحد أقصى، مع تأخير تصاعدي قدره 250ms ثم 500ms. تتم إعادة المحاولة عند أخطاء الشبكة أو رموز 429 و5xx فقط؛ أما أخطاء 4xx الأخرى فتُعاد مباشرة دون تكرار. بعد فشل المحاولتين ينتقل الخادم إلى النموذج/المزود التالي المهيأ في قائمة fallback، ثم يعرض خطأ واضحًا إذا فشل الجميع. لا يعني ذلك تجاوز حدود المزود أو ضمان توفر مجاني دائم.

## الأوامر السريعة

اكتب أحد الأوامر في بداية الرسالة: `/شرح` أو `/تصحيح` أو `/تحسين` أو `/اختبار` أو `/توثيق` أو `/مراجعة`. يزيل الخادم رمز الأمر من النص ويضيف تعليماته إلى prompt النظام تلقائيًا.

## الأمان

تُخزّن مفاتيح API بصيغة AES-256-GCM في قاعدة البيانات، ولا تُعاد قيمتها إلى الواجهة بعد الحفظ. يجب تدوير المفتاح إذا ظهر في سجل أو مستودع بالخطأ. كما تتحقق عمليات المحادثات والحذف من ملكية المستخدم قبل الوصول إلى البيانات.

## الاختبارات

```bash
pnpm check
pnpm test
pnpm build
```

تتضمن الاختبارات تحليل الأوامر العربية، تشفير الأسرار، اختيار المزود والنموذج، وتسجيل الخروج. يظل اختبار الاتصال الفعلي بالمزودين معتمدًا على مفاتيح المستخدم وحصته، لذلك لا تُضمّن مفاتيح حقيقية في الاختبارات.

## فصل مفاتيح المستخدم عن GEMINI_API_KEY العام

المسار الافتراضي للمستخدمين هو إدخال مفاتيح Google أو Groq أو Hugging Face من لوحة **إعدادات المزودين** داخل التطبيق. تُرسل القيم إلى الخادم عبر إجراء محمي، وتُشفّر قبل حفظها في قاعدة البيانات، ولا تُعاد إلى المتصفح بعد الحفظ. متغير `GEMINI_API_KEY` العام هو إعداد خادمي اختياري للاختبار أو التشغيل الإداري، وليس بديلًا عن خزنة مفاتيح المستخدمين ولا يجب وضعه في كود الواجهة.

## التحليلات قبل النشر العام

يحتوي رأس الصفحة على Script تحليلات يعتمد على `VITE_ANALYTICS_ENDPOINT` و`VITE_ANALYTICS_WEBSITE_ID`. قبل النشر العام، يجب إما ضبط المتغيرين إلى خدمة تحليلات يملكها صاحب المشروع والتحقق من سياسة الخصوصية، أو إزالة Script إذا لم تكن التحليلات مطلوبة. لا ينبغي ترك قيم placeholder في النسخة المنشورة.
