# Project TODO

- [x] تثبيت نطاق النسخة الأولى وتوثيق افتراضات التشغيل المجاني دون بطاقة ائتمان
- [x] إعداد مخطط بيانات للمستخدمين والمحادثات والرسائل والملخصات ومفاتيح API
- [x] تطبيق حفظ واسترجاع المحادثات والرسائل والملخصات لكل مستخدم
- [x] تطبيق تخزين مفاتيح API بأمان وعدم كشفها للواجهة أو السجلات
- [x] بناء طبقة موحدة للنماذج Gemini 2.5 Flash وLlama 3.3 70B وMixtral 8x7B
- [x] إضافة التبديل بين مزودي النماذج مع fallback تلقائي عند فشل المزود أو بلوغ الحد
- [x] تطبيق البث التدريجي لردود النماذج
- [x] تطبيق الأوامر العربية /شرح و/تصحيح و/تحسين و/اختبار و/توثيق و/مراجعة تلقائيًا داخل prompt
- [x] بناء واجهة محادثة عربية RTL مع Markdown وتمييز صيغي للكود
- [x] بناء واجهة داكنة تقنية مستوحاة من المخططات الهندسية والشبكات والمعادلات
- [x] إضافة إدارة المحادثات: إنشاء وتحديد وحذف المحادثات القديمة
- [x] إضافة اختيار النموذج وإظهار حالته أثناء المحادثة
- [x] إضافة لوحة إعدادات لمفاتيح API وتفضيلات النموذج
- [x] إضافة تلخيص تلقائي للمحادثات الطويلة وحفظ الملخصات واستخدامها كسياق مستمر
- [x] إضافة حالات التحميل والأخطاء وإعادة المحاولة وتجربة الهاتف وسطح المكتب
- [x] كتابة اختبارات Vitest للـ prompt والمزودات والأمان والمحادثات
- [x] تشغيل فحوص TypeScript والاختبارات والتحقق البصري للواجهة (TypeScript والاختبارات واللقطات نجحت؛ build الإنتاجي نجح باستخدام minify=false)
- [x] توثيق الإعداد والتشغيل باستخدام المفاتيح الشخصية والحدود المجانية
- [x] ربط المشروع بمستودع GitHub وإنشاء commit منظم بعد اكتمال التحقق
- [x] إضافة تلخيص تلقائي للمحادثات الطويلة وحفظ summary وتحديثه واسترجاعه لكل مستخدم
- [x] إضافة fallback تلقائي بين Google وGroq عند الفشل أو نفاد الحصة مع سياسة retry واضحة
- [x] إصلاح حذف المحادثات بالتحقق من الملكية قبل حذف الرسائل وإضافة اختبار صلاحيات
- [x] حفظ تفضيل النموذج لكل مستخدم داخل الإعدادات وربطه بالواجهة
- [x] إضافة زر أو تدفق إعادة المحاولة في الواجهة لمسار البث والأخطاء
- [x] إضافة اختبارات Vitest للمحادثات والإعدادات والعزل بين المستخدمين والبث والتبديل التلقائي
- [x] إجراء تحقق بصري موثق للواجهة بعد فتح التطبيق ومراجعة RTL والحالات الأساسية
- [x] إضافة retry فعلي ومحدد للمزودات داخل streamModel مع توثيق سياسة المحاولة
- [x] إضافة اختبار يثبت أن حذف محادثة لا يحذف بيانات محادثة غير مملوكة
- [x] إضافة تحكم تفضيل النموذج داخل لوحة الإعدادات نفسها
- [x] إضافة اختبارات عزل للمحادثات والإعدادات ومسار SSE/fallback
- [x] تسجيل نتائج التحقق البصري للحالة الفارغة والخطأ وRTL
- [x] توثيق سياسة retry بدقة: محاولتان، تأخير تصاعدي، حالات 429 و5xx، ثم fallback
- [x] استخراج منطق حذف المحادثة المملوكة إلى دالة قابلة لاختبار السلوك الكامل
- [x] إضافة اختبارات إجراءات المحادثات والإعدادات ومنطق fallback المستخدم في SSE
- [x] إضافة اختبارات Vitest لإجراءات conversations وsettings عبر appRouter.createCaller تشمل العزل وتفضيل النموذج
- [x] استخراج قرار اختيار fallback المستخدم في SSE إلى دالة قابلة للاختبار وإضافة اختبار انتقال المزود
- [x] إضافة اختبار عزل ببيانات محادثات فعلية عبر طبقة قابلة للحقن بدل الاعتماد على قائمة فارغة
- [x] استخراج حفظ واسترجاع preferredModel إلى وحدة قابلة للاختبار والتحقق من القيمة بعد الحفظ
- [x] استخراج اختيار fallback الفعلي في SSE إلى دالة مستقلة واختبار فشل المزود الأول ونجاح الثاني
- [x] تحديث توكن تكامل GitHub عبر القناة الآمنة بصلاحية Contents: Read and write للمستودع المحدد
- [x] إعادة رفع checkpoint cf08eb80 إلى sayedelazameydesign-crypto/new-new والتحقق من commit والملفات
- [x] تنظيف الأوامر العربية المكررة في بداية الرسالة بحيث يُحتفظ بأول أمر صالح فقط
- [x] إضافة اختبار regression للحالة `/شرح /شرح ...` مع الحفاظ على بقية النص
- [x] إعادة تشغيل الفحوص والتحقق البصري وحفظ checkpoint جديد بعد الإصلاح
- [ ] حفظ checkpoint جديد بعد إصلاح تنظيف الأوامر العربية المكررة ثم تأكيد رقم النسخة الجديدة في التسليم
